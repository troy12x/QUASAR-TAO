services:
  # Validator API (already deployed at quasar-subnet.onrender.com)
  - type: web
    name: quasar-validator-api
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./quasar.db
    healthCheckPath: /health
